#include <stdio.h>
#include <stdlib.h>

int main()
{
    float la,lo;
    printf("donner la largeur :");
    scanf("%f",&la);
    printf("donner la longueur :");
    scanf("%f",&lo);
    printf("Surface:%f",la*lo);
    return 0;
}
